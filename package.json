{
  "name": "evimeria-serverless",
  "version": "1.0.0",
  "description": "Fonctions serverless pour EVIMERIA e-commerce",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm run build",
    "deploy": "netlify deploy --prod",
    "deploy:functions": "netlify functions:deploy",
    "check:functions": "netlify functions:list"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.9.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.15.0"
  }
}
